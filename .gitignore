# Google TTS API 키 (각자 설정 창에서 넣는다. 절대 올리지 않는다)
tts_api_key.txt

# 개인 설정·사용량 (실행하면 자동 생성)
config.json
usage.json

# 이 컴퓨터에서 만든 음성 캐시 (언제든 다시 만들 수 있음)
# 함께 배포하는 아케르나르 음성은 voices/ 에 따로 있다
audio_cache/

# Python
__pycache__/
*.pyc
